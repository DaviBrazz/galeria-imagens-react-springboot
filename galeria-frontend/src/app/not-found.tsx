'use client'
import { Template } from "@/components"

export default function NotFoundPage() {
    return ( 
    <Template>
        <section>
            <h1 className="text-gray-900 text-xl text-center">Página não encontrada</h1>
        </section>
    </Template>
    )
}